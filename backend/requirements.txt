# VaaniX Backend — pinned dependencies
#
# Pinned to a known-stable set as of VaaniX V1. We deliberately avoid
# the latest-preview versions of any framework because the backend must
# remain deployable from a clean environment without surprises.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
python-multipart==0.0.12
python-jose[cryptography]==3.3.0
supabase==2.7.4
tenacity==9.0.0
python-json-logger==2.0.7

# Test-only — kept in the same file because VaaniX deploys a single
# pinned environment today. Split out into requirements-dev.txt if the
# dependency graph grows.
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
